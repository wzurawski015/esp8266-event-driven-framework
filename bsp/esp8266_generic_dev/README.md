# bsp/esp8266_generic_dev

This is the first board-support placeholder for Stage 2 bring-up.

Framework code outside `bsp/` must not hardcode GPIO numbers.
Board-level pin assignments belong here and may later be generated from SSOT
files.
