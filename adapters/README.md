# adapters

Infrastructure implementations of the ports, including ESP8266 RTOS SDK bindings.
This is the only layer allowed to know SDK details.

Stage 2A1 begins by freezing the adapter boundary and adding the
`adapters/esp8266_rtos_sdk/` placeholder for future ESP8266 RTOS SDK bindings.
